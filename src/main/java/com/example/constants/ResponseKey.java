package com.example.constants;

/**
 * This enum contains the constants for key names to be used in response to the
 * client.
 * 
 * @author ritika.goel
 *
 */
public enum ResponseKey {
	responseTime, status, apiRespCode, apiName, errors, source, message
}
